# Collapse generated lockfiles in diffs / exclude from language stats.
# The lockfile stays TRACKED (CI `npm ci` needs it) — this only hides the noise.
wix-host/package-lock.json linguist-generated=true -diff
